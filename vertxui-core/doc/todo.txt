	- testcase middle-child removal optimalisation + cache CRC64
- /05 to do mvc.com - check elemento https://github.com/hal/elemento
- /08 for existing html-page
	- idea: make multi-registerable assert-js-callbacks for duallanguage tests, not one class TestDOM
- /12 something with http://purecss.io/start/
- /15 webrtc example with 'open source app for android ios windows' like siberiancms.com
- /30: publish: push to maven + spread: info@nljug.org, vertx-awesome, vertx, GWT list, etc.
- daarna: mvcBootstrap bills paginated

OTHER:
- BLOCKING: chatEventbus: receivePOJO does not work in debug=false
- FluentInnerRendering: write tests for all types, and also built for all a check whether the new value is different, otherwise don't take the new value.
- why not put Renderer in Fluent, and then oldView.attr(newView.attr()); etc
- junit: add example with Mockito for Pojofy.ajax
- FigWheely: fix name of compiled .js changes in GWT... + look at sourcefolder not classes
- relocate war->/build/war
